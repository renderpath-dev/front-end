# Preview Verification

1. Run `npm run build`.
2. Run `npm run preview`.
3. Open `http://localhost:4173/`.
4. Confirm the learning shell renders Chapters 01 through 11.
5. Open a Router route from Chapter 06 and refresh it through the configured static fallback when testing a real static server.
